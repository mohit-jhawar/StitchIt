---
import DashboardLayout from '../../../../layouts/DashboardLayout.astro';
import OrderDetail from '../../../../components/orders/OrderDetail';

const user = Astro.locals.user;
if (!user || user.role !== 'CUSTOMER') return Astro.redirect('/dashboard');

const { id } = Astro.params;
if (!id) return Astro.redirect('/dashboard/customer/orders');
---

<DashboardLayout title="Order Detail - StitchIt" pageTitle="Order Detail">
  <div class="space-y-6">
    <a href="/dashboard/customer/orders" class="text-sm text-gray-500 hover:text-indigo-600 transition-colors flex items-center gap-1">
      ← Back to My Orders
    </a>
    <OrderDetail orderId={id} role="CUSTOMER" client:load />
  </div>
</DashboardLayout>
